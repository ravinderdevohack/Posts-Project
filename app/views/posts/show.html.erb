ID: <%= @post.id%>
Title: <%= @post.title %><br>
Description: <%= @post.description %><br>
<% if @post.file.url.present? %>
  Image: <%= image_tag @post.file.url, height:200, width:200  %><br> 
  <%= link_to 'click to download', @post.file.url, download: "" %><br>
<% end %>
<%= button_to 'Home', posts_index_path, method: :get %>
